{
  "name": "cicd-example-project",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Building project...' && mkdir -p dist && cp index.html dist/"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}